#include <bits/stdc++.h>
using namespace std;

void solve(vector<int> vec)
{
  cout<<0;
}

int main()
{
    int n;
    cin>>n;
    vector<int> vec;
    while(n--)
    {
        int x;
        cin>>x;
        vec.push_back(x);
    }
    solve(vec);
}